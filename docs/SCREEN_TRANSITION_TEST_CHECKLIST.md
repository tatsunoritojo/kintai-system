# 画面遷移テストチェックリスト

プロトタイプの全画面遷移が正しく動作するか確認するためのチェックリストです。

---

## ログイン画面 `/login`

| # | 操作 | 期待結果 | OK? |
|---|------|----------|-----|
| 1 | URLに直接アクセス | ログイン画面が表示される | [ ] |
| 2 | ログインボタン押下（プロト用） | `/dashboard` へ遷移 | [ ] |

---

## ダッシュボード `/dashboard`

| # | 操作 | 期待結果 | OK? |
|---|------|----------|-----|
| 1 | 「ダッシュボード」リンク押下 | `/dashboard` へ遷移 | [ ] |
| 2 | 「従業員管理」リンク押下 | `/employees` へ遷移 | [ ] |
| 3 | 「勤務記録」リンク押下 | `/work-records` へ遷移 | [ ] |
| 4 | 「給与計算」リンク押下 | `/payrolls` へ遷移 | [ ] |
| 5 | 「勤務記録を登録」ボタン押下 | `/work-records/new` へ遷移 | [ ] |
| 6 | 「給与を計算」ボタン押下 | `/payrolls/calculate` へ遷移 | [ ] |
| 7 | 「従業員を追加」ボタン押下 | `/employees/new` へ遷移 | [ ] |
| 8 | 「すべて表示」リンク押下 | `/work-records` へ遷移 | [ ] |
| 9 | ユーザーメニュー「設定」押下 | `/settings` へ遷移（未実装でもOK） | [ ] |
| 10 | ユーザーメニュー「ログアウト」押下 | `/login` へ遷移 | [ ] |

---

## 従業員管理

### 従業員一覧 `/employees`

| # | 操作 | 期待結果 | OK? |
|---|------|----------|-----|
| 1 | URLに直接アクセス | 従業員一覧が表示される | [ ] |
| 2 | 「新規登録」ボタン押下 | `/employees/new` へ遷移 | [ ] |
| 3 | 従業員名リンク押下 | `/employees/{id}` へ遷移 | [ ] |
| 4 | 詳細ボタン（目アイコン）押下 | `/employees/{id}` へ遷移 | [ ] |
| 5 | 編集ボタン（ペンアイコン）押下 | `/employees/{id}/edit` へ遷移 | [ ] |

### 従業員登録 `/employees/new`

| # | 操作 | 期待結果 | OK? |
|---|------|----------|-----|
| 1 | URLに直接アクセス | 空のフォームが表示される | [ ] |
| 2 | 「キャンセル」ボタン押下 | `/employees` へ遷移 | [ ] |
| 3 | 「保存」ボタン押下 | `/employees` へ遷移、メッセージ表示 | [ ] |

### 従業員詳細 `/employees/{id}`

| # | 操作 | 期待結果 | OK? |
|---|------|----------|-----|
| 1 | URLに直接アクセス（例: `/employees/emp-001`） | 詳細が表示される | [ ] |
| 2 | 「編集」ボタン押下 | `/employees/{id}/edit` へ遷移 | [ ] |
| 3 | 「一覧へ戻る」ボタン押下 | `/employees` へ遷移 | [ ] |

### 従業員編集 `/employees/{id}/edit`

| # | 操作 | 期待結果 | OK? |
|---|------|----------|-----|
| 1 | URLに直接アクセス | 既存データが入ったフォーム表示 | [ ] |
| 2 | 「キャンセル」ボタン押下 | `/employees` へ遷移 | [ ] |
| 3 | 「保存」ボタン押下 | `/employees` へ遷移、メッセージ表示 | [ ] |

---

## 勤務記録管理

### 勤務記録一覧 `/work-records`

| # | 操作 | 期待結果 | OK? |
|---|------|----------|-----|
| 1 | URLに直接アクセス | 勤務記録一覧が表示される | [ ] |
| 2 | 「勤務記録を登録」ボタン押下 | `/work-records/new` へ遷移 | [ ] |
| 3 | 編集ボタン（ペンアイコン）押下 | `/work-records/{id}/edit` へ遷移 | [ ] |

### 勤務記録登録 `/work-records/new`

| # | 操作 | 期待結果 | OK? |
|---|------|----------|-----|
| 1 | URLに直接アクセス | 空のフォームが表示される | [ ] |
| 2 | 「キャンセル」ボタン押下 | `/work-records` へ遷移 | [ ] |
| 3 | 「保存」ボタン押下 | `/work-records` へ遷移、メッセージ表示 | [ ] |

### 勤務記録編集 `/work-records/{id}/edit`

| # | 操作 | 期待結果 | OK? |
|---|------|----------|-----|
| 1 | URLに直接アクセス（例: `/work-records/wr-001/edit`） | 既存データが入ったフォーム表示 | [ ] |
| 2 | 「キャンセル」ボタン押下 | `/work-records` へ遷移 | [ ] |
| 3 | 「保存」ボタン押下 | `/work-records` へ遷移、メッセージ表示 | [ ] |

---

## 給与計算

### 給与一覧 `/payrolls`

| # | 操作 | 期待結果 | OK? |
|---|------|----------|-----|
| 1 | URLに直接アクセス | 給与一覧が表示される | [ ] |
| 2 | 「給与計算実行」ボタン押下 | `/payrolls/calculate` へ遷移 | [ ] |
| 3 | 「明細」ボタン押下 | `/payrolls/{id}` へ遷移 | [ ] |

### 給与計算実行 `/payrolls/calculate`

| # | 操作 | 期待結果 | OK? |
|---|------|----------|-----|
| 1 | URLに直接アクセス | 計算画面が表示される | [ ] |
| 2 | 「キャンセル」ボタン押下 | `/payrolls` へ遷移 | [ ] |
| 3 | 「計算を開始」ボタン押下 | `/payrolls` へ遷移、メッセージ表示 | [ ] |

### 給与明細詳細 `/payrolls/{id}`

| # | 操作 | 期待結果 | OK? |
|---|------|----------|-----|
| 1 | URLに直接アクセス（例: `/payrolls/emp-001`） | 明細が表示される | [ ] |
| 2 | 「一覧へ戻る」ボタン押下 | `/payrolls` へ遷移 | [ ] |

---

## 共通ナビゲーション

全画面で以下のナビゲーションが正しく動作するか確認：

| # | 操作 | 期待結果 | OK? |
|---|------|----------|-----|
| 1 | ロゴ押下 | `/dashboard` へ遷移 | [ ] |
| 2 | ナビ「ダッシュボード」押下 | `/dashboard` へ遷移 | [ ] |
| 3 | ナビ「従業員管理」押下 | `/employees` へ遷移 | [ ] |
| 4 | ナビ「勤務記録」押下 | `/work-records` へ遷移 | [ ] |
| 5 | ナビ「給与計算」押下 | `/payrolls` へ遷移 | [ ] |

---

**テスト実施日**: ________  
**テスト担当者**: ________  
**結果**: ____/____件 OK
